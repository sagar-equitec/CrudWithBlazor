
@page "/"

<title>User</title>

@using UserManagement.Models
@using UserManagement.Data
@inject NavigationManager NavigationManager
@inject UserService userService
<style>
    .name-hover:hover {
        color: green;
        cursor: pointer;
    }
     .center-text {
        text-align: center;
    }
</style>

<h4 class="popup_title bg_primary">User List</h4>

@if (UsersList == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <div>
        <table class="employee_table">
            <thead>
                <tr>
                    <th width="20%" class="color_title">Name</th>
                    <th width="20%" class="color_title">City</th>
                    <th width="20%" class="color_title">Designation</th>
                    <th width="80%" class="color_title">Skills</th>
                    
                </tr>
            </thead>
            <tbody>
                @foreach (var user in UsersList)
                {
                    <tr @onclick="() => GetUser(user.UserId)">
                        <td width="20%" class="color_title">@user.UserName</td>
                        <td width="20%" class="color_title">@user.UserCity</td>
                        <td width="20%" class="color_title">@user.UserDesignation</td>
                        <td width="80%" class="color_title">@empSkillsDictionary[user.UserId]</td>
                        
                    </tr>
                }
            </tbody>
        </table>
    </div>
}


